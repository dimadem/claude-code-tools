Run the tracing sync script to copy all Claude Code session files for the current project into a local `tracing/` folder:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/sync.sh
```

Report the output to the user.
